// Point this at wherever your guna-pharma-backend server is running.
// Local development default (backend started with `npm start`, PORT=4000):
window.GUNA_CONFIG = {
  API_BASE_URL: 'http://localhost:4000'
};

// When you deploy the backend to a real server, change this to something like:
// window.GUNA_CONFIG = { API_BASE_URL: 'https://api.gunapharma.com' };
